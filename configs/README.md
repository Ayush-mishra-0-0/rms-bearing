# Configuration

Put optional exact incident times in `data/processed/failure_timestamp_overrides.csv` with columns `FailureID,FailureTimestamp` (ISO 8601). Do not commit credentials; use the existing `.env` variables for authorised extraction.
